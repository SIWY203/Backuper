
/* SZKIC PROJEKTU

	- pre-restore snapshot
	- dodać grupowanie folderów do backupów
	- dodać autobackup robiony co X czasu
	- dodać czyszczenie starszych backupów, by utrzymać max liczbę kopii: CleanUp()
	- ZARZĄDZANIE USTAWIENIAMI Z PLIKU !!!
	- dodać możliwość edycji ścieżek w klastrach

	- przenieść z ClusterManager.cs config\clusters.json do nowego ConfigManager.cs
	- przenieść z Loc.cs lang_config.txt do nowego ConfigManager.cs


	---------------

	- dodać do notatek z pattern matching:
	if (UpdateClusterName(c, newName) is { } updated)
	co znaczy "jeżeli wynik metody pasuje do czegokolwiek (czyli != null) to przypisz do zmiennej updated"

*/

